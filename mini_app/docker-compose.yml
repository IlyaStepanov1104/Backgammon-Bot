version: '3.8'

services:
  nextjs:
    build: .
    container_name: nextjs-dev
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
    env_file:
      - .env
    command: npm run dev
